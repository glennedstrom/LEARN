#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, m; cin >> n >> m;

    int h = n - m + 1, l = n / m;
    int top = 1;
    for(int i = h; i > k; i--){top *= i;}
    cout << h << " " << l << endl;
}
