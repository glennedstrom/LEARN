#include <bits/stdc++.h>
using namespace std;

int main()
{
    // SAME EXACT CODE AS multiplication table EXCEPT 3 GROUPS INSTEAD OF 2
    // still code it yourself for practice. It's easy, just implementation-heavy

    // NOTE: You also need to fix up your prime factors snippets and add a prime number generator to make it functional
    // and make it understandable and more written by you. right now it is cryptic
    // 
    
    long long testcases; cin >> testcases;

    for(int testcase = 0; testcase < testcases; testcase++){
        int n; cin >> n;

        // TODO:
    }

    return 0;
}
