#include <bits/stdc++.h>
using namespace std;


int main(){
    int p;
    cin >> p;

    vector<int> piles(p);
    for(int i = 0; i < p; i++){ cin >> piles[i]; }

    int yum;
    cin >> yum;

    vector<int> yummy(yum);
    for(int i = 0; i < yum; i++){cin >> yummy[i];}



}

